import type { ReactNode } from "react"

export const metadata = {
  title: "텍스터브 Embed",
  description: "Embedded text utility tools",
}

export default function EmbedLayout({ children }: { children: ReactNode }) {
  return children
}
